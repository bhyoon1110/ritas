# Development profile
# Git-tracked shared defaults only.
# Server-specific overrides belong in /home/rist/ritas/edge.env.

# Edge API exposed to experiment PCs on the development LAN
EDGE_SERVER_SCHEME=http
EDGE_SERVER_HOST=bhyoon.me
EDGE_SERVER_PORT=8000

# Edge process bind address
EDGE_BIND_HOST=0.0.0.0
